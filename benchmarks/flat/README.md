# Benchmark search on flat index
benchmark flat index search

## to run
```
./benchmark.py
```

This should generate a `benchmark.csv` file, where you can find the raw data, and
a `benchmark.html` file, where you can find the latency plot.
