[project]
name = "dbpedia-openai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
	"pylance",
	"datasets",
]

[dependency-groups]
dev = [ "ruff" ]
