name: run-tests

description: "Install lance wheel and run unit tests"
inputs:
  python-minor-version:
    required: true
    description: "8 9 10 11"

runs:
  using: "composite"
  steps:
  - name: Run python tests
    shell: bash
    working-directory: python
    run: |
      pytest -x -v --durations=30