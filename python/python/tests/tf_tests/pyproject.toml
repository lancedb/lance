[project]
name = "pylance-tf-tests"
version = "0.0.0"
requires-python = ">=3.9,<3.13"
dependencies = [
    "pylance[tests]",
    "tensorflow",
    "ml_dtypes",
    "pandas",
]

[tool.uv.sources]
pylance = { path = "../../../" }

[tool.pytest.ini_options]
addopts = "-m tf_dep"
